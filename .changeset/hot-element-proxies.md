---
"@litsx/core": minor
"@litsx/ssr": minor
---

Add stable development proxies for hydratable custom elements, including scoped registry resolution, so framework dev servers can replace client implementations without redefining registered tags.
