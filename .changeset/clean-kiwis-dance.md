---
"@litsx/typescript-plugin": patch
"@litsx/shiki-languages": patch
---

Remove legacy caret-based hoist metadata from the shared LitSX tooling so editor integrations only surface current `static ... = ...` hoists.
