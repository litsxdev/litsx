---
"@litsx/typescript-plugin": minor
---

Add a shared `editor-session` entrypoint that exposes project-backed LitSX diagnostics, hover, and completions for editor integrations outside the tsserver plugin host.
