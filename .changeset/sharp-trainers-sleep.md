---
"@litsx/core": patch
---

Fix shadow scoped-registry capability detection so LitSX only uses the native shadow registry path when the browser actually upgrades elements created under that registry.
