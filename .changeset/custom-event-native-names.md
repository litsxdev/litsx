---
"@litsx/core": patch
---

Allow authored `@event` handlers to use `CustomEvent` payload types across DOM-named events, custom events with hyphenated names, and custom events with simple names, while preserving useful native DOM event typing for inline handlers.
