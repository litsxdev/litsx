---
"@litsx/core": patch
---

Allow authored `@event` handlers for DOM-named events such as `@input` to use `CustomEvent` payload types while preserving useful native DOM event typing for inline handlers.
