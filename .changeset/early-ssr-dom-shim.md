---
"@litsx/ssr": minor
---

Install one shared SSR DOM identity before Lit evaluates and expose the synchronous `@litsx/ssr/install-dom-shim` bootstrap entry for frameworks that load application components before the main SSR runtime.
