---
"create-litsx-app": patch
---

Make the `app` template the default scaffold, align its starter layout with the current home-style onboarding components, and fix authored `static styles` template output so generated files no longer include legacy `` `); `` hoist closers.
