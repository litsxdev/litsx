---
"create-litsx-app": patch
---

Publish the scaffolded package version refresh so newly generated projects consume the matching `@litsx/core` and compiler versions from the structural hooks release.
