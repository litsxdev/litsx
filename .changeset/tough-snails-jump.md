---
"@litsx/authoring": minor
"@litsx/babel-preset-litsx": minor
"@litsx/compiler": minor
"@litsx/typescript": minor
---

Add implicit `children` projection for LitSX components as a default-slot transform, and report unsupported `children` usages consistently across the compiler and TypeScript tooling. Also extract the shared authored-semantics helpers behind those checks into `@litsx/authoring`.
