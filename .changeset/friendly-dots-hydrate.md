---
"@litsx/core": patch
"@litsx/ssr": patch
---

Fix declarative-shadow-DOM hydration for nested scoped and light-DOM elements, and emit executable SSR bootstrap modules through the Vite dev asset pipeline.
