/**
 * Install Lit's SSR DOM globals before evaluating Lit or server components.
 *
 * This module is synchronous, idempotent, and intended to be imported only
 * for its side effect from a Node.js SSR bootstrap.
 */
export {};
