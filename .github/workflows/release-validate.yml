name: Release Validate

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

concurrency:
  group: release-validate-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: yarn

      - name: Enable Yarn
        run: corepack enable

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Run release validation
        run: |
          yarn release:check
          yarn release:smoke:scaffolds
          yarn release:test

  release-artifacts:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    needs: validate
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: yarn

      - name: Enable Yarn
        run: corepack enable

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Build npm tarballs
        run: yarn release:artifacts

      - name: Build VS Code extension
        run: yarn release:vscode:build

      - name: Package VSIX
        run: yarn release:vscode:package

      - name: Upload npm tarballs
        uses: actions/upload-artifact@v4
        with:
          name: npm-release-artifacts
          path: .release-artifacts/npm/*.tgz

      - name: Upload VSIX
        uses: actions/upload-artifact@v4
        with:
          name: vscode-extension-artifact
          path: packages/vscode-litsx/dist/*.vsix
