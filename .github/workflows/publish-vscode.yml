name: Publish VS Code Extension

on:
  workflow_dispatch:
    inputs:
      pre-release:
        description: Publish as pre-release
        type: boolean
        required: false
        default: false

permissions:
  contents: read
  id-token: write
  attestations: write
  artifact-metadata: write

concurrency:
  group: publish-vscode-${{ github.ref }}
  cancel-in-progress: false

jobs:
  publish:
    name: Publish Extension
    runs-on: ubuntu-latest
    environment: vscode-marketplace
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 20
          package-manager-cache: false

      - name: Enable Yarn
        run: |
          corepack enable
          corepack prepare yarn@4.10.3 --activate

      - name: Cache Yarn state
        uses: actions/cache@v5
        with:
          path: |
            .yarn/install-state.gz
            .yarn-cache
          key: ${{ runner.os }}-yarn4-${{ hashFiles('yarn.lock', '.yarnrc.yml', 'package.json') }}
          restore-keys: |
            ${{ runner.os }}-yarn4-

      - name: Install dependencies
        run: corepack yarn install --immutable

      - name: Validate extension release surface
        run: |
          corepack yarn release:check
          corepack yarn release:vscode:build

      - name: Package VSIX
        run: corepack yarn release:vscode:package

      - name: Attest VSIX
        uses: actions/attest@v4
        with:
          subject-path: packages/vscode-litsx/dist/*.vsix

      - name: Publish extension
        working-directory: packages/vscode-litsx
        env:
          VSCE_PAT: ${{ secrets.VSCE_PAT }}
        run: |
          if [ "${{ github.event.inputs.pre-release }}" = "true" ]; then
            ../../node_modules/.bin/vsce publish --no-dependencies --pre-release
          else
            ../../node_modules/.bin/vsce publish --no-dependencies
          fi
