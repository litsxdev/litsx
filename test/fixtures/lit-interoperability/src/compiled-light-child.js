import { LitElement } from "lit";
import { LightDomMixin } from "@litsx/core/elements";

/** Representative output from an independently compiled LitSX package. */
export class CompiledLightChild extends LightDomMixin(LitElement) {}
